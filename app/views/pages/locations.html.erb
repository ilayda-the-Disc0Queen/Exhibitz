<div class="map-container">
  <div id="map"
       style="width: 60%; height: 600px; margin: 10px 20%;"
       data-markers="<%= @markers.to_json %>"
       data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
</div>
